Redis 缓存最佳实践
1. 缓存场景与收益
Redis 作为内存数据库，常用于缓存热点数据、分布式会话、计数器、排行榜、分布式锁等。合理使用缓存可显著降低数据库压力，提升系统吞吐量和响应速度。

2. 缓存策略选择
Cache-Aside（旁路缓存）：应用程序先查缓存，未命中则查数据库，并回填缓存。适合读多写少场景。

Read-Through / Write-Through：缓存作为代理，应用程序只与缓存交互，缓存负责同步数据库。

Write-Behind（异步写回）：写操作只更新缓存，异步批量写回数据库，提高写入性能，但存在数据丢失风险。

推荐：绝大多数场景使用 Cache-Aside，简单可控。

3. 缓存过期与淘汰机制
设置合理 TTL：根据数据更新频率设定过期时间，避免缓存污染。

Redis 淘汰策略（maxmemory-policy）：

allkeys-lru：全局最近最少使用，适合热点数据。

volatile-lru：仅对设置了 TTL 的 key 进行 LRU。

allkeys-random：随机淘汰，适用于缓存池大小固定。

noeviction：禁止驱逐，写操作报错，适合重要数据。

主动刷新：定时任务预热或刷新即将过期的热点 key，防止击穿。

4. 三大缓存问题及解决方案
4.1 缓存穿透
现象：查询不存在的数据，请求直接打到 DB。

方案：

对不存在的结果也缓存（空值或特殊占位），TTL 设置较短。

使用布隆过滤器（Bloom Filter）预先拦截不存在 key。

4.2 缓存击穿
现象：某个热点 key 过期瞬间，大量并发请求击穿缓存，压垮 DB。

方案：

使用互斥锁（如 SETNX）只允许一个线程重建缓存，其他线程等待。

设置逻辑过期（不物理删除，后台异步更新）。

4.3 缓存雪崩
现象：大量 key 同时过期或 Redis 宕机，导致 DB 压力骤增。

方案：

设置 TTL 时增加随机偏移量，避免集体失效。

高可用部署（主从 + 哨兵 / Cluster），避免单点故障。

开启持久化（RDB + AOF），快速恢复。

5. 数据结构合理选择
String：普通 KV、计数器、分布式锁。

Hash：存储对象字段，支持部分更新。

List：队列、栈、消息队列（LPUSH / RPOP）。

Set：去重、交并差集运算（如共同好友）。

Sorted Set：排行榜、带权重的任务队列。

Bitmap / HyperLogLog：统计活跃用户、UV 等。

6. 性能优化技巧
Pipeline 批量操作：减少网络 RTT。

MGET / MSET：批量获取/设置多个 key。

避免大 key：单个 key 超过 10KB 可能导致网络拥塞，采用拆分或压缩。

使用连接池：复用连接，避免频繁建立关闭。

IO 多路复用：Redis 本身单线程，但可通过多实例分片利用多核。

7. 持久化与高可用
RDB：定期全量快照，适合备份，重启恢复快，但可能丢失最近数据。

AOF：追加写日志，可配置 appendfsync everysec，丢失少于 1s 数据。

混合持久化（Redis 4.0+）：结合 RDB 和 AOF，加快恢复速度。

主从复制：读写分离，主写从读，提升读并发。

Sentinel 哨兵：自动故障转移，保证高可用。

Cluster 集群：数据自动分片（16384 个槽位），支持水平扩展。

8. 监控与运维
使用 INFO 命令查看内存、连接数、命中率。

开启慢日志（slowlog），分析耗时命令。

监控指标：命中率（应 > 95%）、内存使用率（告警 > 80%）、每秒操作数（OPS）、网络带宽。

定期进行 redis-benchmark 压测，评估性能基线。

9. 安全实践
设置强密码（requirepass）。

绑定内网 IP，避免暴露公网。

使用 SSL/TLS 加密传输（Redis 6.0+ 支持）。

禁用危险命令（FLUSHALL、CONFIG 等）或重命名。

10. 代码示例（Python + redis-py）
python
import redis
import time

pool = redis.ConnectionPool(host='localhost', port=6379, password='xxx', max_connections=10)
r = redis.Redis(connection_pool=pool)

def get_user(user_id):
    key = f"user:{user_id}"
    cached = r.get(key)
    if cached:
        return cached.decode()
    # 模拟 DB 查询
    user_data = db.query(user_id)
    if user_data:
        r.setex(key, 3600, user_data)  # 1 小时过期
    else:
        r.setex(key, 60, "null")       # 空值短时缓存
    return user_data
Python 异步编程